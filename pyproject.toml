[project]
name = "upgrade-buddy"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.8.0",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
